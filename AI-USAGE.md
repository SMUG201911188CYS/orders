# ORDERS — AI 활용 기술 문서

NHN NAN 2026 게임 × AI 해커톤 사전 과제 · 1인 제작

> **이 문서가 답하는 것** — AI를 어디에 썼고, **어디에 쓰지 않았고**, AI가 틀렸을 때 무엇으로 잡아냈는가.
> 자랑이 아니라 **재현 가능한 절차**로 적는다. 모든 수치는 저장소의 도구로 다시 잴 수 있고, 명령어를 함께 적었다.

---

## 0. 먼저 밝히는 경계 — 이 게임은 런타임에 AI를 쓰지 않는다

「게임 × AI」 과제이므로 이 구분을 맨 앞에 둔다.

| | AI |
|---|---|
| **게임 실행 중** | **없음.** 네트워크 요청 0건 · 외부 리소스 0건 · API 키 0개 |
| **게임 로직** | **없음.** `Math.random` 0건 — 완전 결정론 |
| **개발 전 과정** | **전부.** 설계 대화 · 구현 · 측정 하네스 · 밸런싱 · 아트 생성 · 문서 |

검증:

```bash
grep -c "Math.random" index.html        # → 0
grep -c "fetch(\|XMLHttpRequest\|<script src\|<link.*href=.http" index.html   # → 0
```

**런타임에 AI를 넣지 않은 것은 역량 부족이 아니라 설계 판단이다.** 이 게임의 핵심 약속은
「같은 지시서는 언제나 같은 밤이 된다」이다. 플레이어가 개입할 수 없는 게임에서 결과가
흔들리면 그것은 퍼즐이 아니라 도박이 된다. LLM을 유닛 행동에 넣는 순간 이 약속이 깨지고,
동시에 **전수 검증도 불가능해진다** — 아래 3장의 방법론 전체가 결정론 위에 서 있다.

대신 **AI를 게임의 주제로 옮겼다.** 플레이어는 자율적으로 움직이는 행위자에게
우선순위 규칙만 내려보내고 실행 중에는 개입할 수 없다. 이것은 에이전트 디렉팅 그 자체다.
(→ 7장)

---

## 1. 도구와 역할 분담

| 도구 | 모델 | 맡은 일 |
|---|---|---|
| **Claude Code** | Opus | 설계 대화 · 구현 · 측정 하네스 작성 · 측정 결과 해석 · 문서 |
| **Codex CLI** | gpt-5.6-sol | 캐릭터 아트 생성 (내장 `image_gen`, EDIT 모드) |
| **사람** | — | **판정 · 기각 · 방향 결정.** 아래 「사람이 한 일」 참조 |

### 사람이 한 일 — AI가 못 한 것

문서 전체에서 가장 중요한 절이다. AI가 만든 것을 **사람이 되돌린 지점**이 설계를 만들었다.

| 사람의 판정 | 무엇을 뒤집었나 |
|---|---|
| *"판 아래 이동은 기존 제가 설계하고자 한 기획에 어긋납니다"* | AI가 제안한 레이아웃 개편을 기각. 3단 구성 확정 |
| *"사소하게 UI의 크기가 변동되는 경험이 저는 불편해서"* | 「내용이 바뀌어도 요소 크기가 안 변한다」를 전역 규칙으로 승격 |
| *"이 아이디어가 아니라 표현이 기각된 것"* (지형 후보) | AI가 「폐기」로 처리하려던 것을 **보류**로 되돌림 |
| *"엄호는 제외하고 A안으로 고정합니다. B안은 인위적입니다"* | AI가 낸 두 안 중 측정상 우열이 없던 것을 취향이 아닌 **설명 가능성**으로 갈랐다 |
| *"왜 생성 이미지를 안 쓰죠?"* | AI가 인물 사진 실험 실패를 픽셀 아트에까지 **과일반화**한 것을 잡아냄 (→ 5장 (e)) |

> AI는 **선택지를 만들고 측정**했고, 사람은 **무엇이 이 게임인지**를 정했다.
> 측정으로 갈리지 않는 것(무엇이 인위적인가, 무엇이 불편한가)은 끝까지 사람의 몫이었다.

---

## 2. 규모

| | |
|---|---|
| 배포물 | `index.html` 한 파일 · 3,554줄 · 504KB · 빌드 없음 |
| **측정 하네스** | **48개 도구 · 4,755줄** — 배포물에 포함되지 않는다 |
| 게임 내 자체 검증 | QA 단언 100여 개 · 시나리오 TC 8개 · 진입점 8종 |
| 커밋 | 26개 / 작업일 6일 (2026-08-09 기준) |

**하네스가 게임 본체의 1.3배 분량이다.** 이 비율이 이 프로젝트의 방법 그 자체다.

---

## 3. 핵심 방법 — AI가 하네스를 쓰고, 하네스가 AI를 기각한다

### 3-1. 왜 이게 가능한가: 결정론

게임에 난수가 없으므로 「승률」은 확률이 아니라 **조합 비율**이다.
카드 순서를 정하면 결과가 하나로 정해지고, 순서를 전부 돌리면 그 밤의 전부를 안다.

그래서 이 프로젝트에서 「밸런스 조정」은 눈으로 보는 일이 아니라 **실행하는 일**이다.
AI가 밸런스를 제안하면, AI가 쓴 하네스가 그 제안을 몇 초 만에 채점한다.

### 3-2. 전수와 표본을 나눈다

이 구분을 흐리면 숫자가 거짓말을 한다.

| | 대상 | 크기 |
|---|---|---|
| **전수** | **셋이 같은 지시서**를 받는 경우 | 밤3 기준 840가지 |
| **표본** | 대원별로 **다른** 지시서를 받는 실제 게임 | 밤3 기준 840³ ≈ **5.9억** → 편성당 800판 표집 |

`?test=1` 은 앞엣것이다. 규칙 위반(판 밖 이탈·겹침·미종료·과회복)을 잡는 데는 전수가 맞고,
난이도는 실제 게임 모드인 뒤엣것으로 재야 한다. **두 모델을 모두 통과해야 채택한다.**

이 규칙이 실제로 후보 하나를 걸러냈다 → 4-4.

### 3-3. 무엇을 재는가

「밤마다 배우는 것이 쌓인다」를 말이 아니라 수치로 정의했다.

| 지표 | 정의 | 이게 없으면 |
|---|---|---|
| **최소 확정 깊이** | 앞 k줄만 정하면 나머지가 뭐든 이기는 최소 k | 깊이 1이면 뒤 3줄은 장식이다 |
| **단일 조건 커버율** | 한 (카드, 슬롯)이 전체 승리에서 차지하는 비중 | 한 수가 승리를 독식해도 총승률로는 안 보인다 |
| **1번 줄 이득 배수** | 최고 1번 줄 승률 ÷ 전체 승률 | 1.0이면 첫 줄을 골라도 이득이 없다 = 배울 게 없다 |
| **4번 줄 사장률** | 이긴 판 중 4번 줄이 한 번도 실행 안 된 비율 | 네 번째 카드를 놓는 행위 자체가 무의미해진다 |

> **총합 승률은 설계를 보여주지 않는다.** 20%가 「고르게 20%」인지
> 「82%가 한 칸에서」인지 총합은 구분하지 못한다. 실제로 초기 3밤이 그 상태였다 —
> 겉보기 승률 20%인데 추격을 1번 줄에 두면 **60전 60승**이었고,
> 그 60판에서 2·3·4번 줄은 **한 번도 실행되지 않았다.**

---

## 4. 증거 — 측정이 AI의 판단을 뒤집은 다섯 건

각 항목은 **AI가 먼저 그럴듯한 답을 냈고, 측정이 그것을 부정한** 사례다.

### 4-1. 나쁜 카드를 고치자 그 카드가 떠받치던 난이도가 무너졌다

「집결」 카드를 정상화(분산 + 교대로 교체)했더니 밤3이 8.1% → **13.3%로 쉬워졌다.**
밤2(10.0%)보다 쉬워져 **난이도 곡선이 역전됐다.**

원인: 집결은 **함정 카드**였다. 96.6%로 조건이 걸리면서 그중 **69.3%가 제자리**였다.
판에 인접 규칙이 없어 「아군 옆으로 간다」가 대개 아무것도 하지 않았다.
그 한 장이 밤3을 어렵게 만들고 있었다.

> **교훈 — 카드 품질과 스테이지 난이도는 따로 재고 따로 맞춰야 한다.**
> 나쁜 카드는 난이도를 「어렵게」가 아니라 「불투명하게」 만든다.

### 4-2. 벽을 늘렸더니 반대로 쉬워졌다

밤3을 조이려고 벽 둘을 추가 → 10%에서 **18%.** 방향이 반대였다.

**벽은 적의 접근도 같이 막는다.** 아군은 거점만 지키면 되고 적은 찾아와야 하므로,
장애물은 구조적으로 수비 측을 돕는다. AI(와 사람)의 직관은 「벽 = 어려움」이었고 틀렸다.

결국 벽이 아니라 **거점 하나를 적진 쪽으로 올려**(`[6,4]→[6,2]`) **4%**로 맞췄다.

| 돌린 손잡이 | 결과 |
|---|---|
| 손패를 6장으로 (분산만 / 교대만) | 12.6% / 12.1% — 오히려 쉬워졌다. **장수는 원인이 아니다** |
| 교체 턴 11 → 8 | 8% |
| 벽 둘 추가 | **18%** ← 반대로 갔다 |
| 아군을 한쪽에 모음 | 11% |
| **거점 하나를 적진 쪽으로** | **4%** ✅ 채택 |

교체 턴은 11턴(밤의 절반)을 **유지했다** — 설명이 깔끔한 값을 배치로 대신 조였다.
「측정만 따르면」 8턴이 나왔지만, 그 값은 플레이어에게 설명할 수 없다.

### 4-3. 버그를 고치자 최강이 최약이 됐다

미오의 재장전 쿨다운이 걸리지 않아 「사거리 5에 매 턴 발사」였다. 정상화하자
평균 승률 14.6% → **6.8%.** 사거리만 낫고 발사·체력·피해가 셋 다 유이보다 아래였기 때문이다.

한 발을 무겁게(피해 2) 하니 **11.7%**로 돌아왔고 밤1 중앙값도 11.1% → 13.9%로 목표 안에 들었다.

> **버그 수정은 밸런스 변경이다.** 고친 뒤 반드시 다시 재야 한다.

### 4-4. 두 모델을 모두 봐야 하는 이유

밤3 변형 규칙(11턴에 허상이 지시서를 갈아 듦) 후보 둘은 대원별 측정에서 **똑같이 8%**였다.
공통줄 모드까지 재자 갈렸다.

| 후보 | 대원별 | 공통줄 | 깊이 |
|---|---|---|---|
| 추격 전환 | 8% | **14%** | **3 → 2** ← 공통줄이 뚫리고 깊이가 얕아진다 |
| **채택** | 8% | 2% | **3 유지** |

한 모델만 봤으면 동전 던지기로 골랐을 것이고, 「누구나 같은 지시서를 주면 쉽게 풀리는 밤」이 됐을 것이다.

### 4-5. 여섯 갈래가 전부 탈락하고, 설계 규칙 셋이 남았다

전위 전용 카드 「저지」의 효과를 다른 것으로 바꿔 보려고 여섯 갈래를 설계하고 전부 측정했다.
**전부 탈락했다.** 그 실패에서 규칙 셋이 나왔다.

| 후보 | 왜 탈락했나 |
|---|---|
| **돌격** (접근+타격) · **밀어붙이기** (타격+넉백) | **공격의 상위 호환.** 조건이 공격을 포함하고 효과가 더 크다 → 공격·추격이 손패에서 죽는다 |
| **반격** | 아군이 **먼저** 행동하므로 먼저 치는 것이 항상 낫다. 전위는 살아 있던 턴의 **86.1%를 안 맞는다** |
| **돌파** (벽 부수기) | **벽은 아군 편이다.** 부수면 자기 방벽을 허문다 (복도 배치 25.3% → 21.8%) |
| **설치** (벽 세우기) | 이점이 측정에 안 나온다. 거점 끊김 14.5% → **16.0%**, 적 점수 29.7 → **31.1** 로 오히려 악화 |
| 선점 · 경계 | 집결 대체 후보. 같은 이유로 탈락 |

여기서 나온 규칙 셋:

1. **새 카드가 기존 카드의 조건을 포함하면서 효과가 같거나 크면 안 된다.** 상위 호환은 그 카드를 손패에서 지운다
2. **「~하면 반응한다」는 카드는 그 사건의 빈도부터 재라** (→ 5장 (f)의 51.2% 오독이 바로 여기서 나왔다)
3. **판을 바꾸는 카드는 22턴 중 한 턴의 값과 경쟁해야 한다.** 이 게임의 한 턴은 「거점 한 칸」이거나 「적 체력 1」인데, 7×7 에는 늘 돌아갈 길이 있어 벽 하나가 그보다 크기 어렵다

> **부수기와 세우기가 양쪽 다 실패한 이유가 같다** — 벽 하나의 값이 한 턴의 값보다 작다.
> 여섯 번의 실패가 한 번의 성공보다 쓸 게 많았다.
> 측정이 없었으면 여섯 중 하나를 「그럴듯하다」는 이유로 채택했을 것이다.

---

## 5. AI가 틀리는 방식과, 그것을 잡은 장치

이 장이 이 문서의 본론이다. **AI는 틀린다. 문제는 틀리는 방식이 일정하다는 것이고,
일정하면 장치로 막을 수 있다.**

### (a) 자기보고를 믿으면 안 된다 → 출력 계약으로 강제한다

아트 생성을 맡긴 Codex는 실패했을 때도 성공했다고 답하는 경향이 있었다.
그래서 프롬프트 끝에 **구조화 출력 계약**을 박았다.

```
1. MODE:  어떤 도구, 어떤 모드, 입력 이미지를 실제로 넘겼는가 (yes/no)
2. FILES: 출력 경로 — 실제 바이트 크기 — 존재 여부 (yes/no)
3. NOTES: 등신이 유지됐는가 · 픽셀이 격자를 지켰는가
```

**바이트 크기와 존재 여부를 직접 적게 하면** 파일을 안 만들고 성공했다고 말하기 어려워진다.
그리고 그 보고조차 믿지 않고 **디코딩해서 픽셀을 직접 검사했다** — 실제로 한 번은
보고가 「정상」인데 머리색이 강조색으로 읽히고 있었다.

### (b) 텍스트만으로는 비율을 못 잡는다 → 기준 이미지를 실제 입력으로 넘긴다

캐릭터 등신을 글로만 지시했을 때 결과가 **1.8~3.8등신 사이에서 흔들렸다.**
「2등신으로」라고 아무리 적어도 지켜지지 않는다.

해결은 **EDIT 모드** — 기준 이미지를 *읽게* 하는 게 아니라 *실제 입력 이미지로 넘긴다.*
그러자 비율·발밑선·가로 중심이 보존됐다.

> **역할이 갈린다.** 비율·구도는 사람이 기준 이미지로 못 박고, 그 안의 형태는 생성이 채운다.
> 실제로 20칸 안에서 팔·손·다리를 분리하는 일은 **생성 쪽이 손보다 나았다.**

### (c) 조용히 틀리는 변환 → 생성 전에 왕복부터 검증한다

생성된 PNG를 게임의 팔레트 글리프로 되돌리는 변환기를 만들었다.
**토큰을 쓰기 전에**, 이미 정답을 아는 기준 이미지를 넣었다 빼서 원본과 한 칸도 다르지 않은지 먼저 확인했다.

이 검사가 세 건을 미리 잡았다. **셋 다 에러 없이 조용히 틀리는 종류다.**

| 잡은 것 | 검사 없었으면 | 실제 원인 |
|---|---|---|
| 배경키가 한 대원의 머리를 삼킴 | *"생성이 이상하게 나왔다"* 로 오진 | 배경색과 거리 270 < 투명 판정 반경 900 → **머리 41칸 소실** |
| 하이라이트 = 눈 흰자 | *"피격 연출이 좀 이상하다"* | 두 글리프의 색이 **동일**. 피격이 흰자를 살색으로 바꿔 백발이 번짐 |
| 강조색이 머리로 샘 | 은발이 크림색으로 | 강조와 은백이 근접 |

근접 글리프 쌍은 79개나 됐지만 **위험한 건 눈 흰자가 낀 쌍뿐이었다.**
스프라이트는 자기 팔레트로 되돌려 자기 팔레트로 그리므로, 색이 비슷한 두 글리프가
뒤바뀌어도 화면은 거의 같다 — **스스로 상쇄된다.**
흰자만 예외인 이유는 피격 프레임이 흰자를 *다른 색으로 바꾸기* 때문이다.

> 이 구분이 없었으면 79개를 다 고치려 들었을 것이다.
> **「문제로 보이는 것」과 「실제로 결과를 바꾸는 것」을 가르는 일이 절반이다.**

### (d) 검사 자체가 틀릴 수 있다

「1번 줄이 승패와 무관함」을 잡는 불변식이 **이긴 적 있는 카드만** 비교하고 있었다.
1번 줄에 두면 한 번도 못 이기는 카드는 분모에서 통째로 빠졌는데, **하필 그게 배울 게 가장 많은 카드다.**

결과는 양방향 오류였다.

- **오탐** — 4장이 균등하고 나머지 1장이 0승인 판을 「무관함」으로 잘못 걸렀다
- **미탐** — 추격 60/60 지배는 반대 방향이라 **못 잡았다**

지금은 0승 카드까지 분모에 넣은 이득 배수와 단일 조건 커버율 **두 개로 양쪽을 다 본다.**

> **측정을 믿는 방법론의 최대 약점은 측정 코드다.**
> 하네스가 통과시켰다는 사실 자체를 정기적으로 의심해야 한다.

### (e) AI는 과일반화한다 — 그리고 근거까지 붙여 방어한다

인물 사진 스타일 생성이 24px 칸에서 뭉개진 실험을 근거로,
AI(나)는 **「생성 이미지는 판 위에 쓸 수 없다」**로 일반화하고 README에 **그 이유까지 적어 두었다.**

사람이 *"왜 생성 이미지를 안 쓰죠?"* 라고 물어 뒤집혔다. 실제로는
사진 스타일 WebP와 픽셀 아트를 혼동한 것이었고, 브리프는 처음부터 둘을 병행할 계획이었다.

> **사실이 낡은 것보다, 낡은 판단을 근거까지 붙여 논증해 둔 쪽이 고치기 어렵다.**
> AI가 쓴 문서를 정기적으로 사실과 대조해야 하는 이유다.
> 실제로 이번에 README를 전면 개정하면서 5×5·16턴·「생성 이미지 안 씀」이 한꺼번에 드러났다.

### (f) 그 밖에 반복해서 확인된 것

| 증상 | 대응 |
|---|---|
| 통계를 잘못 읽어 단언 | 「전위 피격 51.2%」는 개인 피격률이 아니라 **아군 피격 중 점유율**이었다(턴당 13.9%). 지표 정의를 문서에 못 박음 |
| 코드를 안 보고 단언 | 「대기 로그가 없다」는 첫 커밋부터 존재했다. **주장 전 grep** |
| 자기 실험 조건을 잊음 | 「생성 카드가 24px에서 뭉개진다」는 내가 LANCZOS로 줄인 탓. NEAREST면 깨끗하다 |
| 크기 추정을 감으로 | 「스프라이트는 문자열로 두는 게 작다」 → 실측하니 반대였다. 지금 `SPR` 블록은 **2,779 B**, 같은 내용의 1배 PNG 시트는 **약 715 B**(base64로 인라인해도 ~950 B). **이미지가 3배 작다** |

**공통점 — 재보면 5초 걸리는 것을 재지 않고 말했다.** 그래서 이 프로젝트의 규칙은
「주장하기 전에 명령어를 한 줄 돌린다」가 됐다.

---

## 6. 재현

```bash
# 게임 자체 검증 (브라우저에서)
index.html#qa=1      # 기능 QA — 통과하면 제목이 QA-PASS
index.html#test=1    # 카드 순열 전수 — fail 이 빈 배열이면 통과
index.html#ui=1      # DOM 배선 (실제 click 으로 개시까지)
index.html#fuzz=200&seed=7   # 무작위 조작 + 매 스텝 불변식

# 밸런스 재측정 (Node, tools/ 는 저장소 바깥)
node sweep.js foeorder 3 1500   # 허상 지시서 1번 칸의 지배력
node sweep.js twist 3 1500      # 밤3 변형 규칙 후보 (두 모델 동시)
node roster.js B 800            # 84편성 × 밤별 800판
node cardstat.js                # 카드별 발동률·제자리 비율 ← 집결을 잡은 도구
node slots.js                   # 네 칸 중 실제로 발동한 칸

# 아트 파이프라인
node sprref.js                  # 기준 이미지 생성
node png2spr.js <png> 20 <키>   # PNG → 글리프 (배경키 거리·격자 자체 검사)
node palaudit.js                # 복원 불가능한 글리프 쌍 감사
```

`?a=b` 와 `#a=b` 를 둘 다 받는다. 크롬은 `file://` 에 쿼리스트링이 붙으면 로드를 거부하므로
로컬 파일은 해시를 쓴다.

---

## 7. 본선 과제와의 연결

본선은 **「에이전트 설계서」**와 **「디렉팅 명세서」**를 요구한다.
ORDERS는 그 둘을 문서가 아니라 **게임 규칙 자체로** 구현하고 있다.

| 본선 개념 | ORDERS에서 |
|---|---|
| 에이전트 | 아홉 대원. 각자 다른 능력치와 역할 전용 행동을 갖는다 |
| 디렉팅 명세 | **지시서** — 우선순위가 매겨진 조건→행동 4줄 |
| 실행 중 개입 불가 | 개시 후 밤이 끝날 때까지 아무것도 할 수 없다 |
| 관측으로만 얻는 정보 | 허상의 지시서는 **발동한 줄만** 한 칸씩 공개된다 |
| 명세 품질의 평가 | 승률이 아니라 **확정 깊이 · 단일 커버율 · 사장률** |

플레이어가 게임에서 배우는 것은 **「자율 행위자에게 우선순위를 어떻게 주는가」**다.
그리고 이 문서 5장이 보여주듯, 그것은 제작 과정에서 **내가 실제로 배운 것**이기도 하다.

---

## 8. 한계 — 정직하게

- **AI 활용의 정량 측정을 하지 않았다.** 「AI가 코드의 몇 %를 썼는가」 같은 수치는
  적지 않았다. 세지 않았고, 사후에 추정한 값을 적으면 이 문서 전체의 기준과 어긋난다
- **아트 아홉 중 넷이 미완이다.** 생성 도구의 사용 한도에 걸려 중단됐다.
  나머지 넷은 같은 역할군의 몸을 빌리고 팔레트만 갈아끼운다
- **편성 84개 중 30개는 밤1이 밤3보다 어렵다.** 손대지 않기로 결정했다 —
  모든 편성에 곡선을 맞추려면 편성 간 차이를 지워야 하고, 그러면 「편성이 답을 가른다」가 사라진다
- **하네스는 저장소 바깥에 있다.** 배포물을 한 파일로 유지하기 위한 선택인데,
  그 결과 심사 시점에 하네스를 직접 확인할 수 없다. 위 6장의 수치는 이 점을 감안해 읽어야 한다

---

> **문서 형식에 관한 가정** — 공고가 요구하는 세부 형식(분량·확장자)을 확인하지 못해,
> 어떤 형식으로 제출하든 통하도록 자립 문서로 작성했다. 지정 양식이 있으면 이 내용을 옮기면 된다.

# ORDERS · AI 활용 기술 문서

NHN NAN 2026 게임 × AI 해커톤 사전 과제 · 1인 제작

이 문서는 제작에 사용한 **AI 도구**, 실제로 사용한 **프롬프트**, 그리고 **활용 내역**을 정리한 것입니다.

---

## 1. 사용한 AI 도구

| 도구 | 모델 | 실행 방식 | 담당한 일 |
|---|---|---|---|
| **Claude Code** | Opus | 대화형 CLI | 게임 구현 · 측정 하네스 작성 · 밸런스 조정 · 문서 작성 |
| **Codex CLI** | gpt-5.6-sol | 비대화형 (`codex exec`) | 캐릭터 아트 생성 · 판 위 스프라이트 생성 |

### 1-1. Claude Code

프로젝트 전 과정에서 사용했습니다. 게임 본체(`index.html`)와 저장소 바깥의 측정 하네스(`tools/`)를
모두 이 도구로 작성했습니다.

작업 단위는 대화이며, 매 세션에 프로젝트 지시사항이 자동으로 주입됩니다(2-1 참조).

### 1-2. Codex CLI

이미지 생성에만 사용했습니다. **내장 `image_gen` 도구를 사용하므로 OpenAI API 키가 필요하지 않습니다.**
`scripts/image_gen.py` 경로로 내려가면 키를 요구하므로, 프롬프트에 그 경로를 쓰지 말라고 명시했습니다.

| 항목 | 값 |
|---|---|
| 버전 | Codex v0.146.0 |
| 모델 | gpt-5.6-sol |
| 추론 강도 | xhigh |
| 승인 모드 | never (무인 실행) |
| 샌드박스 | workspace-write |

**한 번에 한 명씩 실행했습니다.** 4장씩 묶어 실행하면 래퍼 프로세스만 종료되고
`codex.exe`가 고아 프로세스로 남는 현상을 확인했기 때문입니다.

---

## 2. 프롬프트

### 2-1. 상시 프롬프트 (프로젝트 지시사항)

개별 요청과 별개로, 모든 세션에 자동으로 주입되는 지시사항을 두었습니다.
같은 규칙을 매번 다시 적지 않아도 되고, 세션이 바뀌어도 작업 방식이 유지됩니다.

주입한 규칙 중 결과에 크게 작용한 것은 다음 다섯 가지입니다.

| 규칙 | 목적 |
|---|---|
| 아이디어 요청에는 즉시 구현하지 말고 검토와 기획안을 먼저 낼 것 | 방향이 어긋난 채로 코드가 쌓이는 것을 막습니다 |
| 작동 증명 없이 완료로 표시하지 말 것 | 실행 결과를 붙이지 않은 보고를 걸러냅니다 |
| 비자명한 작업은 계획을 먼저 세우고, 틀어지면 즉시 멈추고 재기획할 것 | 잘못된 전제 위에서 밀어붙이는 것을 막습니다 |
| 리서치와 탐색은 서브에이전트에 위임할 것 | 주 대화의 문맥을 깨끗하게 유지합니다 |
| 교정을 받으면 그 패턴을 기록해 둘 것 | 같은 실수의 반복을 줄입니다 |

### 2-2. 게임 로직 프롬프트

게임 규칙과 화면은 대화로 만들었기 때문에 아트처럼 정형화된 문면이 없습니다.
대신 **요청의 순서를 고정**했습니다.

```
① 요청       무엇을 원하는지와, 그것이 왜 필요한지
② 기획안     AI 가 선택지와 근거를 냅니다. 이 단계에서는 코드를 쓰지 않습니다
③ 승인 또는 기각
④ 구현       승인된 안만 코드로 옮깁니다
⑤ 측정       하네스를 돌려 의도한 값이 나왔는지 확인합니다
```

②와 ④를 **단어로 구분**했습니다. 「검토해 주세요」, 「가능한지 파악해 주세요」는 조사와
기획안만 요구하며 코드를 금지합니다. 「진행해 주세요」, 「구현해 주세요」에서만 구현이 시작됩니다.
이 구분이 없으면 아이디어를 말한 즉시 코드가 생겨서, 방향을 바꿀 때 버릴 것이 많아집니다.

실제로 사용한 요청의 예시입니다.

| 종류 | 프롬프트 |
|---|---|
| 규칙 설계 | 밤마다 허상 지시서를 다르게 하고, 편성별로 정답이 갈리게 설계하고, 밤3에 변형 규칙을 넣어 주세요. 최소 확정 깊이는 「다」 정의로 재 주세요. |
| 신규 카드 | 전위 전용 카드 「저지」의 효과를 다른 것으로 바꿔 봅시다. 기존 규칙에서 가져오는 것이 아니라 아이디어 회의입니다. 전위라는 개념에 맞는 행동이 무엇이 있을지 고려해 주세요. |
| 근거 요구 | 벽을 세우는 것이 어떤 이점을 발생시키는지 제시해 주세요. |
| 기각 | 판 아래로 옮기는 것은 제가 설계하려던 기획에 어긋납니다. |
| 범위 한정 | 카드 문안은 미정으로 두고, 다른 작업을 모두 끝낸 뒤에 진행합시다. |

**설계안을 낼 때는 그것을 잴 방법도 같이 내도록 요구했습니다.** 이 게임은 난수가 없어
카드 순서를 전부 실행해 볼 수 있으므로, 제안과 측정 결과를 같은 응답에서 받을 수 있습니다.
그래서 「재미있을 것 같다」로 끝나는 제안이 남지 않습니다.

기각도 프롬프트의 일부로 취급했습니다. 위 표의 「기각」과 「근거 요구」처럼,
받아들이지 않은 이유를 한 줄로 남기면 같은 제안이 다시 올라오지 않습니다.

### 2-3. 아트 생성 프롬프트

Codex에 넘긴 프롬프트는 정형화되어 있습니다. 아래는 판 위 스프라이트 생성에 사용한
실제 프롬프트의 구조이며, 캐릭터마다 이름과 머리 모양 기술만 바뀝니다.

```
<task>
imagegen 스킬의 기본 내장 image_gen 도구를 EDIT 모드로 사용할 것.
scripts/image_gen.py 를 사용하지 말 것. CLI 경로를 사용하지 말 것.
편집 호출은 정확히 한 번만 할 것.

입력 이미지 두 장을 실제 이미지로 넘길 것 (읽기만 하지 말 것):

  이미지 1 (편집 대상, 480x480): spr-src/C_sora.png
    20x20 픽셀 아트를 24배로 확대한 것. 비율과 자세가 정답이며 고정 대상.

  이미지 2 (정체성 참조, 480x480): spr-src/P_sora.png
    같은 인물의 32x32 흉상. 머리 모양과 머리 색만 읽을 것.
    이 이미지의 구도나 비율은 절대 따르지 말 것. 머리를 키우지 말 것.

이미지 1에서 그대로 보존할 것:
  - 등신 비율: 약 2등신. 머리가 위 절반, 몸이 아래 절반.
  - 머리 크기와 화면상 위치. 머리를 키우지 말 것.
  - 발이 닿는 기준선과 좌우 중앙 정렬.
  - 정면을 향해 서 있는 대기 자세, 팔은 옆으로 내리고 다리는 벌린 상태.
  - 평평한 순청색 배경 #0000F0. 크로마키이므로 완전히 균일하게 유지할 것.

바꿀 것 (편집의 목적):
  - 이 인물 고유의 머리 모양을 줄 것: 짧은 은백색 단발.
  - 상의는 연두색 작업 재킷. 전위 역할로 읽힐 것.
  - 몸이 실제 신체로 읽히게 할 것: 어깨, 손이 보이는 팔, 허리, 발이 있는 두 다리.

픽셀 격자 고정 (가장 중요한 제약):
  이 이미지는 20x20 픽셀 아트를 24배로 확대한 것이다.
  보이는 모든 픽셀은 20x20 격자에 정확히 정렬된 24x24 크기의 단색 블록이어야 한다.
  격자 한 칸보다 작은 요소, 안티앨리어싱, 그라데이션, 흐림, 그림자를 넣지 말 것.
  자가 검사: 최근접 이웃 방식으로 정확히 20x20 으로 축소했을 때
  형태가 동일해야 하며 사라지거나 번지는 부분이 없어야 한다.

제약:
  - 화면상 60x60 픽셀에서 읽혀야 한다 (판에서 3배로 그려진다).
  - 글자, 숫자, 테두리, 무기, 소품을 넣지 말 것.
  - 480x480 으로 출력할 것.

출력 경로: spr-out/sora.png
</task>

<action_safety>
위 출력 파일 하나를 만드는 것 외에 아무것도 변경하지 말 것.
</action_safety>

<verification_loop>
답하기 전에 출력 파일이 디스크에 실제로 존재하는지 확인하고 실제 바이트 크기를 보고할 것.
내장 image_gen 도구를 쓸 수 없거나 실패하면 즉시 중단하고 그 사실을 보고할 것.
scripts/image_gen.py 나 API 키 경로로 대체하지 말 것.
</verification_loop>

<structured_output_contract>
정확히 다음 형식으로만 답할 것:
1. MODE:  어떤 도구, 어떤 모드, 입력 이미지를 실제로 넘겼는지 (yes/no)
2. FILES: 출력 경로 · 실제 바이트 크기 · 존재 여부 (yes/no)
3. NOTES: 등신 비율이 유지되었는지 · 머리가 바뀌었는지 · 격자를 지켰는지
</structured_output_contract>
```

**여섯 명분의 프롬프트를 손으로 쓰지 않았습니다.** `tools/sprprompt.js`가 인물 정보 표
한 줄씩만 받아 나머지 문면이 글자 하나까지 같은 프롬프트를 찍어냅니다.
손으로 쓰면 문구가 조금씩 달라지고, 달라진 만큼 결과가 흔들립니다.

### 2-4. 아트 프롬프트에 반드시 넣은 네 가지

| 항목 | 넣지 않으면 |
|---|---|
| **경로 지정** (`scripts/image_gen.py`를 쓰지 말 것) | CLI 경로로 내려가 API 키를 요구하며 멈춥니다 |
| **보존 목록** (비율 · 기준선 · 중앙 정렬) | 등신이 1.8에서 3.8 사이로 흔들립니다 |
| **격자 고정과 자가 검사 문장** | 안티앨리어싱된 가짜 픽셀이 섞여 축소가 불가능해집니다 |
| **구조화 출력 계약** | 파일을 만들지 않고도 성공했다고 답합니다 |

특히 **기준 이미지를 실제 입력 이미지로 넘기는 EDIT 모드**가 결정적이었습니다.
글로만 "2등신으로" 라고 적었을 때는 결과가 1.8등신에서 3.8등신 사이에서 흔들렸고,
편집 경로로 바꾼 뒤에야 비율이 고정되었습니다.

---

## 3. 활용 내역

### 3-1. 산출물별 정리

| 산출물 | 도구 | 내용 |
|---|---|---|
| 게임 본체 `index.html` | Claude Code | 규칙 엔진 · 화면 · 자체 검증 전부 |
| 측정 하네스 `tools/` 50개 | Claude Code | 카드 순열 전수 실행, 편성별 승률, 카드 발동률, 스프라이트 변환 |
| 밸런스 조정 | Claude Code | 스테이지 배치 · 손패 구성 · 대원 수치 |
| 캐릭터 아트 | Codex CLI | 아래 3-2 |
| 문서 | Claude Code | README · 게임 소개서 · 본 문서 · 시연 영상 콘티 |

### 3-2. 생성한 아트 내역

| 산출물 | 수량 | 생성 방식 | 게임에서 쓰이는 곳 |
|---|---:|---|---|
| 캐릭터 카드 아트 | 9 | `image_gen` EDIT | 아래 두 항목의 원본 |
| 편성 화면 전신 이미지 | 9 | 카드 아트를 `gen_fullshot.py`로 가공 | 편성 화면 |
| 32×32 초상 | 9 | 카드 아트에서 `gen_sprites.py`로 추출 | 편성 화면 · 야근조 패널 |
| 타이틀 키 비주얼 | 1 | `image_gen` | 타이틀 화면 |
| 판 위 스프라이트 20×20 | 5 / 9 | `image_gen` EDIT 후 `png2spr.js`로 글리프 변환 | 판 위 |

초상과 전신 이미지를 따로 생성하지 않고 **카드 아트 한 장에서 파생**시켰습니다.
같은 그림에서 나오므로 화면마다 인물이 달라 보이는 일이 생기지 않습니다.

판 위 스프라이트는 생성 결과를 그대로 쓰지 않고 **게임의 팔레트 글리프 문자열로 되돌립니다.**
색을 그대로 두면 캐릭터별 색 교체가 동작하지 않기 때문입니다.

**아홉 중 다섯까지 완료했습니다.** 나머지 넷은 Codex 사용 한도에 걸려 중단되었으며,
현재는 같은 역할군의 몸을 빌리고 팔레트만 교체해 표시하고 있습니다.

### 3-3. 규모

| 항목 | 값 |
|---|---|
| 배포물 `index.html` | 3,554줄 · 504KB (생성 이미지 base64 약 300KB 포함) |
| 측정 하네스 | 50개 파일 · 5,246줄 (배포물에 포함되지 않음) |
| 게임 내 자체 검증 | 단언 100여 개 · 시나리오 TC 8개 · 진입점 8종 |
| 아트 프롬프트 파일 | 68개 |
| 아트 실행 로그 | 22개 |
| 커밋 | 30개 / 작업일 6일 |

### 3-4. 생성 결과를 확인한 방법

AI의 자기 보고를 그대로 신뢰하지 않았습니다.

| 대상 | 확인 방법 |
|---|---|
| 아트 생성 | 보고된 파일 크기와 존재 여부를 대조한 뒤, PNG를 디코딩해 픽셀을 직접 검사 |
| 스프라이트 변환 | 정답을 아는 기준 이미지를 변환기에 넣었다 빼서 원본과 일치하는지 먼저 확인 |
| 밸런스 변경 | 카드 순열 전수 실행과 편성별 표집을 다시 수행 |
| 게임 코드 | `?qa=1` 기능 검증, `?ui=1` 화면 배선, `?fuzz=N` 무작위 조작 시 불변식 검사 |

실제로 아트 생성 보고가 정상이었으나 디코딩해 보니 머리 색이 강조색으로 읽히고 있던 사례,
변환기가 특정 인물의 머리를 배경으로 오인해 41칸을 통째로 잃던 사례를
이 절차에서 미리 발견했습니다.

---

## 4. 참고 자료 위치

| | |
|---|---|
| 아트 프롬프트 전문 | `orders/art/_SP_*.md`, `_E*.md`, `_C_*.md` |
| 아트 실행 로그 | `orders/art/_SPL_*.log`, `_EL*.log` |
| 프롬프트 생성기 | `tools/sprprompt.js` |
| 스프라이트 변환기 | `tools/png2spr.js`, `tools/applyspr.js` |
| 측정 하네스 | `tools/*.js` |
